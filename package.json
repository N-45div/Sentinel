{
  "name": "x402-sentinel",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "servers/*",
    "sdk",
    "agents/*",
    "tools/*"
  ],
  "scripts": {
    "build": "npm -ws run build",
    "dev": "npm -ws run dev"
  }
}
